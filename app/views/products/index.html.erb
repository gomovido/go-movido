<div class="container products-container"data-controller="filters">
  <figure class="big-blue-bubble position-absolute"></figure>
  <div class="step-1">
    <%= render 'subscriptions/steps', current_step: 1, title: t('.title'), subtitle: t('.subtitle'), product: @products.first, total_steps: @products.first.total_steps, congratulations: false %>
    <%= render 'product-details'%>
    <div class="row pt-5">
      <div class="col-lg-4  d-flex justify-content-start">
        <%= render 'filters', products: @products %>
      </div>
      <div class="col-lg-8">
        <span class="d-none no-result" data-filters-target="noResult"><%= t('.no_result').html_safe %></span>
        <div class="products-cards-wrapper">
          <% @products.each do |product| %>
            <% if product.is_wifi? %>
              <%= render 'wifi-card', product: product %>
            <% elsif product.is_mobile?%>
              <%= render 'mobile-card', product: product %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
