<div class="content p-lg-5 p-3 d-flex justify-content-between">
  <div class="left">
    <div class="d-sm-flex align-items-center ">
      <%= image_tag(asset_path(@product.logo_url), class:'mr-4 mb-3 mb-sm-0 logo') %><br>
      <span class='main-title'><%= @product.company %> <%= @product.name %></span>
    </div>
    <button type="button" class="close position-absolute d-none d-md-block" data-dismiss="modal" aria-label="Close">
      <i class="far fa-times-circle"></i>
    </button>
    <div class="features-wrapper mt-5">
      <% @product.product_features.each do |feature, index| %>
      <div class="feature mb-4">
        <p class="title"><strong><%= feature.name %></strong></p>
        <span class='desc'><%= feature.description %></span>
      </div>
      <% end %>
    </div>
  </div>
  <div class="right d-flex flex-column align-items-center justify-content-between justify-content-sm-around mt-4">
    <div class="pricing d-flex flex-column align-items-center w-100">
      <span class='price text-center'><%= @product.format_price %></span>
      <span class="sub-price text-center"><%= t('.monthly') %></span>
      <%= link_to t('.cta').html_safe, category_product_subscriptions_path(@product.category, @product), class:'btn-card m-b2 w-100 mt-3', method: :post %>
    </div>
    <% if @product.is_mobile? %>
      <div class="features-lite-wrapper">
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><%= image_tag('products/modal/wifi-purple.png', class:"picto") %></span>
          <span class="desc"><%= @product.data_limit %></span>
        </div>
        <hr>
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><%= image_tag('products/modal/sim-card-purple.png', class: "picto") %> </span>
          <span class="desc"><span class="black"><%= @product.format_sim_card_price %></span> <%= t('.sim_card') %></span>
        </div>
        <hr>
        <% if @product.obligation %>
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><%= image_tag('products/modal/contract-purple.png', class: 'picto') %></span>
          <span class="desc"><span class="black"><%= @product.time_contract %></span> <%= t('.months') %></span>
        </div>
        <% else %>
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><%= image_tag('products/modal/contract-purple.png', class: 'picto') %></span>
          <span class="desc"><%= t('.no_obligation').html_safe %></span>
        </div>
        <% end %>
      </div>
    <% elsif @product.is_wifi? %>
      <div class="features-lite-wrapper">
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><%= image_tag(asset_path('products/modal/wifi-purple.png'), class:'picto ') %></span>
          <div class="d-flex flex-column align-items-center"><span class="mr-1 font-weight"><%=@product.data_speed %></span> <span>Ø <%= t('products.card.speed') %></span></div>
        </div>
        <hr>
        <div class="feature-lite d-flex flex-column align-items-center">
          <span><i class="fas fa-plug mb-2 purple"></i> </span>
          <span class="desc"><%= @product.format_setup_price %></span>
          <span><%= t('products.card.setup_costs') %></span>
        </div>
      </div>
    <% end %>
  </div>
</div>
