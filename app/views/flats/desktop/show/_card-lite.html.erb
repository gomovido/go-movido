<%= link_to flat_path(location, type, code), class: 'w-100' do  %>
  <div class="flat-card-lite">
    <div class="img-box mb-2" style="background-image:url(<%= image %>);"></div>
    <div>
      <div class="name mb-2"><%= name.size > 50 ? name.first(50) + '...' : name %></div>
      <div class="pricing"><%= price %> /<%= billing.downcase %></div>
    </div>
  </div>
<% end %>
