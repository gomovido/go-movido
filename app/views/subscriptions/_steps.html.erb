<% if browser.device.mobile? %>
  <%= render 'subscriptions/mobile/steps', title: title, subtitle: subtitle %>
<% else %>
  <%= render 'subscriptions/desktop/steps', product: product, current_step: current_step, title: title, subtitle: subtitle, total_steps: total_steps, congratulations: congratulations %>
<% end %>
