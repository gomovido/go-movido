<div class="loader d-none" id="loader"><div class="lds-hourglass"></div></div>
<div class="container subscription-container d-flex flex-column align-items-center">
  <div class="row">
    <div class="col-md-12">
      <%= render 'subscriptions/steps', product: @subscription.product, current_step: @subscription.current_step(controller.controller_name), title: t('orders.new.title'), subtitle: t('orders.new.subtitle', price: @subscription.format_amount).html_safe, total_steps: @subscription.product.total_steps, congratulations: false %>
    </div>
    <%= render 'subscriptions/mobile/stripe' %>
  </div>
</div>
