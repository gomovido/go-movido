<div class="container profile-container my-4" data-controller="profile" data-profile-locale-value="<%= I18n.locale.to_s %>"  data-profile-readonly-class="readonly" data-profile-hide-class="d-none" data-profile-flex-class="d-flex">
  <h2 class="text-center mb-4"><%= t('.title') %></h2>
    <div class="tabs text-center mb-4">
      <ul class="nav nav-pills d-flex align-items-center justify-content-center" id="pills-tab" role="tablist">
        <li class="nav-item ml-0">
          <a class="nav-link <%= params[:active_tab] ? '' : 'active' %>" id="pills-subscriptions-tab" data-toggle="pill" href="#pills-subscriptions" role="tab"><%= t('.subscriptions_tab') %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= params[:active_tab] == 'addresses' ? 'active' : '' %>" id="pills-addresses-tab" data-toggle="pill" href="#pills-addresses" role="tab"><%= t('.addresses_tab') %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= params[:active_tab] == 'profile' ? 'active' : '' %>" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab"id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab"><%= t('.profile_tab') %></a>
        </li>
      </ul>
    </div>
  <div class="tab-content row" id="pills-tabContent">
    <%= render 'users/profil', user: @user %>
    <%= render 'users/subscriptions', subscriptions: @subscriptions %>
    <%= render 'users/addresses', address: @address, active_address: current_user.active_address, addresses: current_user.addresses.where(active: false) %>
  </div>
</div>
