<div class="why-card d-flex flex-column align-items-center mb-3">
  <div class="illustration p-2 mb-2">
    <%= image_tag(asset_path(img)) %>
  </div>
  <div class="d-flex flex-column text-center">
    <span class="title mb-2"><strong><%= title %></strong></span>
    <span class="desc"><%= desc %></span>
  </div>
</div>
